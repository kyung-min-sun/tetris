import {ReactNode} from 'react';

/**
 * Webpage Header
 * @return {ReactNode}
 */
export function Header(): ReactNode {
  return <div className="bg-gray">
    <div>
      ISO Tank
    </div>
  </div>;
}
